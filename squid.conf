# Only allow outbound requests to Mistral and GitHub. Everything else is denied.
acl allowed_dst dstdomain .mistral.ai api.github.com github.com
http_access allow allowed_dst
http_access deny all
http_port 3128
